---
name: openehr-context
description: When working with openEHR (archetypes, templates, AQL, ADL, CKM, compositions), load guides from the MCP server before answering or editing.
alwaysApply: false
globs: ["**/*.adl", "**/*.adls", "**/*.oet", "**/*.t.json", "**/*.opt", "**/*.optx", "**/*.optj", "**/*.aql"]
---

# openEHR context

When the workspace involves openEHR (archetypes, templates, AQL, ADL, CKM, compositions, OPT, terminology):

1. **Guide-first**: Load relevant implementation guides from the openEHR Assistant MCP server (`guide_search` / `guide_get`) before creating or editing archetypes, templates, AQL, or composition formats.
2. **MCP tools**: Use `mcp__openehr-assistant__*` tools for CKM search, guide access, terminology, type specs, ADL idioms, and curated examples as needed.
3. **Slash invocations**: Prefer the openEHR Assistant commands and user-invocable skills when they fit the task (e.g. `/ckm-search`, `/openehr-explain`, `/semantic-diff`, `/archetype-impact`); they encode guide-first workflows.
