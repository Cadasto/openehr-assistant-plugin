# Vale configuration for this repository's human-facing prose.
#
# Vale (https://vale.sh) checks style, not spelling. CI runs
# `vale --minAlertLevel=error .` in the `prose` job. Locally:
#
#   vale sync          # download the style packages named below into StylesPath
#   vale .             # lint the repo
#
# Adapted from the docs-editing reference config. StylesPath is gitignored
# (the packages are downloadable, not source). The vocabulary stays in-repo.

StylesPath = styles
MinAlertLevel = suggestion

# Google's developer-documentation style guide is the closest public match to the
# house style, and its rules are well-maintained. write-good catches passive voice,
# weasel words and hedges; alex catches insensitive phrasing; proselint catches
# clichés and redundancy. Microsoft's package is an alternative to Google's — enable
# one or the other, never both (their rules contradict on person and contractions).
Packages = Google, write-good, alex, proselint

# Project terminology and accepted spellings. Populate
# styles/config/vocabularies/Cadasto/{accept,reject}.txt — `accept.txt` is the
# term list that keeps identifiers and domain names from being "corrected",
# and `reject.txt` is where banned marketing vocabulary goes so the linter,
# not a reviewer, catches it.
Vocab = Cadasto

[*.{md,mdx,markdown}]
BasedOnStyles = Vale, Google, write-good, alex, proselint

# --- Deliberate deviations from the Google package -------------------------------
# British spelling in prose; code identifiers keep their own spelling.
Google.Spelling = NO
# Semantic line breaks are welcome here, so sentence-per-line triggers false positives.
Google.Parens = NO
# The house style permits spaced em dashes for parenthetical breaks.
Google.EmDash = NO
# Headings are sentence case, which Google's package already enforces — keep it on.
Google.Headings = YES
# "we" is disallowed in reference docs but legitimate in marketing copy; enforce by
# review rather than globally, since Vale cannot tell the two contexts apart.
Google.We = suggestion

# E-Prime objects to every form of "to be", which reference documentation is built on.
# It is a writing exercise, not a style rule.
write-good.E-Prime = NO
# Flags domain vocabulary this repo cannot avoid: "hook", "failure".
alex.ProfanityUnlikely = NO

# --- Escalations -----------------------------------------------------------------
# These are errors rather than suggestions: future tense where present belongs,
# weasel words, and condescending hedges.
Google.Will = error          # future tense where present tense belongs
write-good.Weasel = error    # "very", "really", "basically"
write-good.Illusions = error # "simply", "just", "obviously"
alex.Condescending = error   # "easy", "simply", "of course"

# Content that is not ours to restyle. The changelog's preamble is Keep a Changelog
# boilerplate; section settings merge, so the escalations above are unset here.
[{CHANGELOG.md}]
BasedOnStyles = Vale
Google.Will = NO
write-good.Weasel = NO
write-good.Illusions = NO
alex.Condescending = NO
Google.Headings = NO
Google.We = NO

# Legal and community text — flag it in review, never lint it.
[{LICENSE,CODE_OF_CONDUCT.md}]
BasedOnStyles = 
Vale.Spelling = NO
Google.Will = NO
write-good.Weasel = NO
write-good.Illusions = NO
alex.Condescending = NO
Google.Headings = NO
Google.We = NO

# Plugin content addresses a model, not a reader: skills, agents, commands,
# Cursor rules, and the AI-assistant briefs. Read them for terminology;
# do not restyle them.
[{skills/**,agents/**,commands/**,rules/**,hooks/**,AGENTS.md,.claude/**,.github/copilot-instructions.md}]
BasedOnStyles = 
Vale.Spelling = NO
Google.Will = NO
write-good.Weasel = NO
write-good.Illusions = NO
alex.Condescending = NO
Google.Headings = NO
Google.We = NO

# Vendored style packages are downloads, not our prose.
[styles/**]
BasedOnStyles = 
Vale.Spelling = NO
Google.Will = NO
write-good.Weasel = NO
write-good.Illusions = NO
alex.Condescending = NO
Google.Headings = NO
Google.We = NO
